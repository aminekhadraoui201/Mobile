<!-- table_border.xml -->
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/white"/> <!-- Inside area -->
    <stroke android:color="@android:color/darker_gray" android:width="1dp"/> <!-- Gray border -->
    <corners android:radius="4dp"/> <!-- Optional rounded corners -->
</shape>
